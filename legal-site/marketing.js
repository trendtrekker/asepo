const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches,items=document.querySelectorAll('.reveal');if(reduced)items.forEach(x=>x.classList.add('show'));else{const o=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('show');o.unobserve(e.target)}}),{threshold:.12});items.forEach(x=>o.observe(x))}const h=document.querySelector('header');addEventListener('scroll',()=>h.classList.toggle('stuck',scrollY>24),{passive:true});
